rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global Safety Net (Allowing all reads and writes in Development Sandbox)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
